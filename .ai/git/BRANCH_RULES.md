# BRANCH RULES

Preferred collaboration model:
- Backend and Frontend may work on separate owner branches.
- Do not force-push shared branches.
- Do not rewrite published history.
- Pull/rebase before checkpoint commits when safe.
- If the repository's existing branch strategy differs, inspect it first and preserve the established strategy.

The agent must never delete or reset another owner's work to make its own checkpoint pass.
